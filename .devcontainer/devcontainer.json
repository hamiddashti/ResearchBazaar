{
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "settings": {
      "workspaceFolder": "." // Current directory as workspace
  },
  "extensions": [
      "ms-python.python" // Python extension
  ],
  "postCreateCommand": "conda env create -f environment.yml && conda activate rb2024" // Install and activate conda environment
}