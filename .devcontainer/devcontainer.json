{
  "image": "mcr.microsoft.com/devcontainers/miniconda:latest",
  "settings": {
      "workspaceFolder": "."
  },
  "extensions": [
      "ms-python.python"
  ],
  "postCreateCommand": "conda env create -f environment.yml && conda init bash && echo 'conda activate rb2024' >> ~/.bashrc"
}
